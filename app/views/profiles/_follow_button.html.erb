<% if following?(user) %>
  <%= button_to "Unfollow", profiles_unfollow_path(id: @user.id), method: :delete, class:"btn btn-danger shadow" %>
<% else %>
  <%= button_to "Follow", profiles_follow_path(id: @user.id), method: :post, class:"btn btn-primary shadow" %>
<% end %>
